import { ThemeHighlight } from "../../components/syntechHighlight/theme";

# Theme Customization with Tailwind CSS

Hikma UI is built with Tailwind CSS, allowing for easy customization and consistent styling across your application.

## Setting up Tailwind CSS

To use Hikma UI components with proper styling, you'll need to set up Tailwind CSS in your project. Follow the [Tailwind CSS installation guide](https://tailwindcss.com/docs/installation) to get started:


## Customizing the Theme

Hikma UI provides a default theme that you can customize to match your application's design. You can override the theme variables in your `tailwind.config.js` file:

```js

<ThemeHighlight />